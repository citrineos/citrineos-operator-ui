# Phase 0 Playwright E2E environment template.
# Copy to `.env.test` (gitignored) and fill in real values.

E2E_BASE_URL=http://localhost:3000
HASURA_URL=http://localhost:8090/v1/graphql
CITRINE_CORE_URL=http://localhost:8080
E2E_TENANT_ID=1
E2E_ADMIN_EMAIL=admin@example.com
E2E_ADMIN_PASSWORD=replace-me
E2E_AUTH_PROVIDER=generic

# Optional. Phase 0 leaves blank; Phase 2 schema-drift guard uses it if set.
HASURA_ADMIN_SECRET=
